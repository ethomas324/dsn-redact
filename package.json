{
  "name": "dsn-redact",
  "version": "0.1.0",
  "description": "Redact credentials from database and service connection strings",
  "private": true,
  "bin": {
    "dsn-redact": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test dist"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
